// Activates the Carousel
$('.carousel').carousel({
    interval: 4000
})

// Activates Tooltips for Social Links
$('.tooltip-social').tooltip({
    selector: "a[data-toggle=tooltip]"
})

$(function(){
    document.title = document.title + " | GPIG-C"
});

